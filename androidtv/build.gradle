// Root build file. Plugin versions are pinned so the GitHub Actions build is
// deterministic (AGP 8.5 requires Gradle 8.7+, JDK 17).
plugins {
    id 'com.android.application' version '8.5.2' apply false
}
